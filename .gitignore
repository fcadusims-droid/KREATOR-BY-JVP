# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# E1 run outputs (rankings are regenerated; don't commit them)
out/
*.ranking.json

# Media — never commit raw gameplay or model weights
data/videos/
*.mp4
*.mov
*.mkv
*.wav
models/
*.safetensors
*.gguf

# Web frontend uploads/outputs
web/_work/

# project-pipeline orchestrator run state (transient per-session working state)
.pipeline/

# K Library media (real licensed assets, not code)
assets/music/*
assets/sfx/*
assets/memes/*
!assets/**/.gitkeep
