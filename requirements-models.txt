# Kreator — optional model backends for the Evidence Layer (K Analyst).
#
# These are needed ONLY to attach VLM/ASR labels to signal-flagged slices on a
# GPU box. E1 (ranking quality) can be validated signal-only without them; add
# them when you want the VLM to confirm events and lift event-confidence scores.
#
#   pip install -r requirements.txt -r requirements-models.txt

torch>=2.1
transformers>=4.49          # Qwen2.5-VL (Apache-2.0)
accelerate>=0.30
whisperx>=3.1               # word-level ASR (BSD-2)
